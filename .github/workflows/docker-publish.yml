name: Publish to DockerHub

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

on:
#   schedule:
#     - cron: '42 7 * * *'
  push:
    branches: [ main ]
    # Publish semver tags as releases.
    tags: [ 'v*.*.*' ]
    paths-ignore:
#       - ".github/**"
      - "*.md"
      - "LICENSE"
#   pull_request:
#     branches: [ main ]
#     paths-ignore:
#       - ".github/**"
#       - "*.md"
#       - "LICENSE"

# env:
#   # Use docker.io for Docker Hub if empty
#   REGISTRY: docker.io
#   # github.repository as <account>/<repo>
#   IMAGE_NAME: ninerealmlabs/jupyter-docker-stacks


# jobs:
#   build:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       packages: write
# #     strategy:
# #       matrix:
# #         image: ["base-notebook", "minimal-notebook", "scipy-notebook", "datascience-notebook"]
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v2
#         with:
#           repository: jupyter/docker-stacks

#       - name: Convert to buildx
#         uses: jacobtomlinson/gha-find-replace@master
#         with:
#           find: "docker build $(DARGS)"
#           replace: "docker buildx $(DARGS)"
#           include: "{{ $GITHUB_WORKSPACE }}/Makefile"

#       # Validate change
#       - uses: pCYSl5EDgo/cat@master
#         id: cat
#         with:
#           path: "{{ $GITHUB_WORKSPACE }}/Makefile"

#       - run: echo $TEXT
#         env:
#           TEXT: ${{ steps.cat.outputs.text }}


#       - name: Set up QEMU
#         uses: docker/setup-qemu-action@v1


#       # Login against a Docker registry except on PR
#       # https://github.com/docker/login-action
#       - name: Log into registry ${{ env.REGISTRY }}
#         # if: github.event_name != 'pull_request'
#         uses: docker/login-action@v1
#         with:
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_TOKEN }}

#       # Build and push Docker image with Buildx (don't push on PR)
#       # https://github.com/docker/build-push-action
#       - name: Build and push Docker image
#         uses: docker/build-push-action@v2
#         with:
#           context: .
#           platforms: linux/arm64,linux/amd64
#           push: ${{ github.event_name != 'pull_request' }}
#           tags: ${{ steps.meta.outputs.tags }}
#           # labels: ${{ steps.meta.outputs.labels }}
